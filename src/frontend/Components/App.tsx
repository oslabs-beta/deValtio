import { SideBarContainer } from './SideBar/SideBarContainer';

const App = (): JSX.Element => {
    return (
        <div>
            <SideBarContainer />
        </div>
    );
}

export default App;