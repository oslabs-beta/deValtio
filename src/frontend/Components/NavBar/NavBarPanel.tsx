import { INavBarPanelProps } from "../../../Types/Types";

export const NavBarPanel = ({ children, value, index }: INavBarPanelProps): JSX.Element => {
    return (
        <div>
            NavBarPanel
        </div>
    )
}