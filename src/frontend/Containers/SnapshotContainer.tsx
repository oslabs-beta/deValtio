import Grid from '@material-ui/core/Grid';

export const SnapshotContainer = (): JSX.Element => {
    return (
        <Grid container item>
            This is where the snapshot goes 
        </Grid>
    );
}
